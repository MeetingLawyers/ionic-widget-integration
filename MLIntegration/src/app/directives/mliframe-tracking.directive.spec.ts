import { MLIframeTrackingDirective } from './mliframe-tracking.directive';

describe('MLIframeTrackingDirective', () => {
  it('should create an instance', () => {
    const directive = new MLIframeTrackingDirective();
    expect(directive).toBeTruthy();
  });
});
